import { ClampDirective } from './clamp.directive';

describe('ClampDirective', () => {
  it('should create an instance', () => {
    const directive = new ClampDirective();
    expect(directive).toBeTruthy();
  });
});
